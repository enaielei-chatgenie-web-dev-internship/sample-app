
import $ from "jquery";

$(() => {
    // $('.menu > .item').on('click', function() {
    //     $(this).parent().children(".item").removeClass("active");
    //     $(this).addClass('active');
    // }); 
});